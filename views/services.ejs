<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Our Services - Home Service Booking</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-light">
  <div class="container mt-4">
    <div class="row justify-content-center mb-4">
      <div class="col-md-10 text-center">
        <div class="card border-0 shadow-sm">
          <div class="card-body">
            <h2 class="text-primary">Our Services 🔧</h2>
            <p class="text-muted">Choose from our range of professional home services.</p>
          </div>
        </div>
      </div>
    </div>
    
    <% if (services.length === 0) { %>
      <div class="alert alert-info text-center">
        <h4>No services available yet!</h4>
        <p class="mb-0">Check back soon or contact support.</p>
      </div>
    <% } else { %>
      <div class="row g-4">
        <% services.forEach(service => { %>
          <div class="col-md-4">
            <div class="card h-100 shadow border-0">
              <div class="card-body d-flex flex-column">
                <h5 class="card-title"><%= service.name %></h5>
                <p class="card-text flex-grow-1"><%= service.description || 'Reliable service for your home needs.' %></p>
                <p class="text-success fw-bold fs-5 mb-3">₹<%= service.price %> / Service</p>
                <a href="/booking?serviceId=<%= service._id %>" class="btn btn-primary w-100 mt-auto">Book Now 📅</a>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    <% } %>

    <div class="text-center mt-5">
      <a href="/" class="btn btn-outline-primary">Back to Home 🏠</a>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
