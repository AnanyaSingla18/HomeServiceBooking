<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Booking Confirmed - Home Service Booking</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-light">
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <a class="navbar-brand" href="/">🏠 Home Services</a>
      <div class="navbar-nav ms-auto">
        <% if (user) { %>
          <span class="navbar-text me-3">Welcome, <%= user %>!</span>
          <a class="nav-link" href="/services">Services</a>
          <a class="nav-link" href="/bookings">My Bookings</a>
          <a class="nav-link" href="/logout">Logout</a>
        <% } else { %>
          <a class="nav-link" href="/services">Services</a>
          <a class="nav-link" href="/login">Login</a>
          <a class="nav-link" href="/signup">Sign Up</a>
        <% } %>
      </div>
    </div>
  </nav>

  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-8 text-center">
        <div class="card shadow border-success">
          <div class="card-header bg-success text-white">
            <h3>✅ Booking Confirmed!</h3>
            <p class="mb-0">Your service has been scheduled successfully.</p>
          </div>
          <div class="card-body">
            <% if (user) { %>
              <p class="lead">Great job, <%= user %>! Here's your booking details:</p>
            <% } else { %>
              <p class="lead">Here's your booking details:</p>
            <% } %>
            
            <div class="row">
              <div class="col-md-6">
                <h5>Service: <%= booking.service.name %></h5>
                <p><strong>Price:</strong> ₹<%= booking.service.price %></p>
              </div>
              <div class="col-md-6">
                <h5>Date: <%= new Date(booking.date).toLocaleDateString() %></h5>
                <p><strong>Time Slot:</strong> <%= booking.timeSlot.charAt(0).toUpperCase() + booking.timeSlot.slice(1) %> </p>
              </div>
            </div>
            
            <div class="row mt-3">
              <div class="col-md-12">
                <h5>Customer: <%= booking.customerName %></h5>
                <p><strong>Contact:</strong> 
                  <% if (booking.contactMethod === 'email') { %>
                    📧 <%= booking.email %>
                  <% } else { %>
                    📞 <%= booking.phone %>
                  <% } %>
                </p>
              </div>
            </div>

            <div class="mt-4">
              <a href="/bookings" class="btn btn-primary me-2">View My Bookings 📋</a>
              <a href="/services" class="btn btn-outline-primary">Book Another Service</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer class="bg-dark text-white text-center py-3 mt-5 fixed-bottom">
    <p>&copy; 2024 Home Service Booking. All rights reserved.</p>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
