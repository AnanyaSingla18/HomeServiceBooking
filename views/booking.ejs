<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Book Service - Home Service Booking</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-light">
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <a class="navbar-brand" href="/">🏠 Home Services</a>
      <div class="navbar-nav ms-auto">
        <% if (user) { %>
          <span class="navbar-text me-3">Welcome, <%= user %>!</span>
          <a class="nav-link" href="/services">Services</a>
          <a class="nav-link" href="/bookings">My Bookings</a>
          <a class="nav-link" href="/logout">Logout</a>
        <% } else { %>
          <a class="nav-link" href="/services">Services</a>
          <a class="nav-link" href="/login">Login</a>
          <a class="nav-link" href="/signup">Sign Up</a>
        <% } %>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card shadow">
          <div class="card-header bg-primary text-white text-center">
            <h3>📅 Book Your Service</h3>
            <% if (service) { %>
              <p class="mb-0">Service: <%= service.name %> - ₹<%= service.price %></p>
            <% } %>
          </div>
          <div class="card-body">
            <% if (error) { %>
              <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <%= error %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
              </div>
            <% } %>

            <form method="POST" action="/booking">
              <input type="hidden" name="serviceId" value="<%= serviceId %>">
              
              <div class="mb-3">
                <label for="customerName" class="form-label">Customer Name</label>
                <input type="text" class="form-control <%= error && customerName ? 'is-invalid' : '' %>" 
                       id="customerName" name="customerName" value="<%= customerName %>" required>
                <div class="form-text">We'll use your account name by default.</div>
              </div>

              <div class="mb-3">
                <label for="date" class="form-label">Preferred Date</label>
                <input type="date" class="form-control <%= error && date ? 'is-invalid' : '' %>" 
                       id="date" name="date" value="<%= date %>" required min="<%= new Date().toISOString().split('T')[0] %>">
                <div class="invalid-feedback">Please select a future date.</div>
              </div>

              <div class="mb-3">
                <label class="form-label">Contact Method</label>
                <div class="form-check">
                  <input class="form-check-input" type="radio" name="contactMethod" id="emailRadio" value="email" <%= contactMethod === 'email' ? 'checked' : '' %> required>
                  <label class="form-check-label" for="emailRadio">Email</label>
                </div>
                <div class="form-check">
                  <input class="form-check-input" type="radio" name="contactMethod" id="phoneRadio" value="phone" <%= contactMethod === 'phone' ? 'checked' : '' %>>
                  <label class="form-check-label" for="phoneRadio">Phone</label>
                </div>
              </div>

              <!-- Email Field (toggle) -->
              <div class="mb-3 email-field" style="<%= contactMethod !== 'email' ? 'display: none;' : '' %>">
                <label for="email" class="form-label">Email Address</label>
                <input type="email" class="form-control <%= error && email ? 'is-invalid' : '' %>" 
                       id="email" name="email" value="<%= email %>" <%= contactMethod === 'email' ? 'required' : '' %>>
                <div class="invalid-feedback">Please enter a valid email.</div>
              </div>

              <!-- Phone Field (toggle) -->
              <div class="mb-3 phone-field" style="<%= contactMethod !== 'phone' ? 'display: none;' : '' %>">
                <label for="phone" class="form-label">Phone Number</label>
                <input type="tel" class="form-control <%= error && phone ? 'is-invalid' : '' %>" 
                       id="phone" name="phone" value="<%= phone %>" <%= contactMethod === 'phone' ? 'required' : '' %> placeholder="+91 9876543210">
                <div class="invalid-feedback">Enter a valid Indian phone number (10 digits).</div>
              </div>

              <div class="mb-3">
                <label for="timeSlot" class="form-label">Time Slot</label>
                <select class="form-select <%= error && timeSlot ? 'is-invalid' : '' %>" id="timeSlot" name="timeSlot" required>
                  <option value="">Choose a time...</option>
                  <option value="morning" <%= timeSlot === 'morning' ? 'selected' : '' %>>Morning (9 AM - 12 PM)</option>
                  <option value="afternoon" <%= timeSlot === 'afternoon' ? 'selected' : '' %>>Afternoon (12 PM - 4 PM)</option>
                  <option value="evening" <%= timeSlot === 'evening' ? 'selected' : '' %>>Evening (4 PM - 8 PM)</option>
                </select>
                <div class="invalid-feedback">Please select a time slot.</div>
              </div>

              <button type="submit" class="btn btn-success w-100">Confirm Booking ✅</button>
            </form>

            <div class="text-center mt-3">
              <a href="/services" class="btn btn-outline-secondary">Back to Services</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Toggle Script -->
  <script>
    document.querySelectorAll('input[name="contactMethod"]').forEach(radio => {
      radio.addEventListener('change', function() {
        const emailField = document.querySelector('.email-field');
        const phoneField = document.querySelector('.phone-field');
        if (this.value === 'email') {
          emailField.style.display = 'block';
          phoneField.style.display = 'none';
          document.getElementById('email').required = true;
          document.getElementById('phone').required = false;
        } else {
          emailField.style.display = 'none';
          phoneField.style.display = 'block';
          document.getElementById('phone').required = true;
          document.getElementById('email').required = false;
        }
      });
    });
  </script>

  <!-- Footer -->
  <footer class="bg-dark text-white text-center py-3 mt-5 fixed-bottom">
    <p>&copy; 2024 Home Service Booking. All rights reserved.</p>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
