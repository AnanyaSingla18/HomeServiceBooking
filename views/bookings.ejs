<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Bookings - Home Service Booking</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-light">
  <div class="container mt-4">
    <div class="row justify-content-center">
      <div class="col-md-12">
        <div class="card shadow border-0 rounded-4">
          <div class="card-header bg-info text-white text-center py-3">
            <h2 class="mb-0">📋 All Bookings</h2>
            <p class="mb-0">View scheduled services and details.</p>
          </div>
          <div class="card-body p-0">
            <% if (bookings.length === 0) { %>
              <div class="p-5 text-center">
                <img src="https://images.unsplash.com/photo-1556742049-0cfed4f6a45d?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80" alt="No Bookings" class="img-fluid mb-3 rounded" style="max-height: 200px; opacity: 0.7;">
                <h4 class="text-muted">No bookings yet!</h4>
                <p>Start by <a href="/services">browsing services</a>.</p>
              </div>
            <% } else { %>
              <div class="table-responsive">
                <table class="table table-hover mb-0">
                  <thead class="table-dark">
                    <tr>
                      <th>Service</th>
                      <th>Customer</th>
                      <th>Date</th>
                      <th>Time</th>
                      <th>Contact</th>
                      <th>Status</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% bookings.forEach(booking => { %>
                      <tr>
                        <td>
                          <% if (booking.service) { %>
                            <strong><%= booking.service.name %></strong><br>
                            <small class="text-success">₹<%= booking.service.price %></small>
                          <% } else { %>
                            <span class="text-danger">Service Unavailable</span>
                          <% } %>
                        </td>
                        <td><%= booking.customerName %></td>
                        <td><%= new Date(booking.date).toLocaleDateString('en-IN') %></td>
                        <td>
                          <span class="badge bg-warning text-dark px-3 py-2">
                            <%= booking.timeSlot.charAt(0).toUpperCase() + booking.timeSlot.slice(1) %>
                          </span>
                        </td>
                        <td>
                          <% if (booking.contactMethod === 'email') { %>
                            <span class="badge bg-primary">📧 <%= booking.email %></span>
                          <% } else { %>
                            <span class="badge bg-success">📞 <%= booking.phone %></span>
                          <% } %>
                        </td>
                        <td>
                          <span class="badge bg-success">Confirmed</span><br>
                          <small class="text-muted">Booked: <%= new Date(booking.createdAt).toLocaleDateString('en-IN') %></small>
                        </td>
                      </tr>
                    <% }); %>
                  </tbody>
                </table>
              </div>
            <% } %>
          </div>
          <div class="card-footer text-center py-3">
            <a href="/services" class="btn btn-primary me-2">Book New Service 🔧</a>
            <a href="/" class="btn btn-outline-secondary">Back to Home 🏠</a>
          </div>
        </div>
      </div>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
