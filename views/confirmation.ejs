<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Booking Confirmed - Home Service Booking</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="/">HouseMate</a>
      <ul class="navbar-nav ms-auto">
        <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
        <li class="nav-item"><a class="nav-link" href="/services">Services</a></li>
        <li class="nav-item"><a class="nav-link" href="/bookings">Bookings</a></li>
      </ul>
    </div>
  </nav>

  <!-- Main Content -->
  <main class="container my-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card border-success shadow-lg">
          <div class="card-header bg-success text-white text-center">
            <h2 class="mb-0">✅ Booking Confirmed!</h2>
            <p class="mb-0">Your service is scheduled. We'll reach out soon.</p>
          </div>
          <div class="card-body text-center">
            <h4 class="text-success mb-3">Thank you, <%= booking.customerName %>!</h4>
            
            <!-- Service Details -->
            <div class="mb-4">
              <h5><i class="bi bi-tools text-primary"></i> Service: <%= booking.service.name %></h5>
              <p class="text-muted">Price: ₹<%= booking.service.price %></p>
            </div>
            
            <!-- Booking Details -->
            <div class="mb-4">
              <h5><i class="bi bi-calendar text-primary"></i> Date: <%= new Date(booking.date).toLocaleDateString('en-IN') %></h5>
            </div>

            <!-- Contact Preference & Details -->
            <div class="mb-4">
              <h5><i class="bi bi-<%= booking.contactMethod === 'email' ? 'envelope' : 'telephone' %> text-primary"></i> 
                Contact Via: <%= booking.contactMethod === 'email' ? '📧 Email' : '📞 Phone' %>
              </h5>
              <% if (booking.email) { %>
                <p class="text-muted"><strong>Email:</strong> <%= booking.email %></p>
              <% } %>
              <% if (booking.phone) { %>
                <p class="text-muted"><strong>Phone:</strong> <%= booking.phone %></p>
              <% } %>
              <p class="text-muted small">We'll confirm your booking through this method.</p>
            </div>

            <!-- NEW: Preferred Time Slot -->
            <div class="mb-4">
              <h5><i class="bi bi-clock text-primary"></i> Time Slot: 
                <% if (booking.timeSlot === 'morning') { %>Morning (9 AM - 12 PM)<% } 
                   else if (booking.timeSlot === 'afternoon') { %>Afternoon (12 PM - 4 PM)<% } 
                   else if (booking.timeSlot === 'evening') { %>Evening (4 PM - 8 PM)<% } 
                   else { %>Not specified<% } %>
              </h5>
              <p class="text-muted small">We'll aim for this time on your selected date.</p>
            </div>

            <div class="d-grid gap-2 d-md-block">
              <a href="/services" class="btn btn-primary me-md-2 mb-2">Book Another Service</a>
              <a href="/bookings" class="btn btn-outline-secondary">View My Bookings</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer class="footer fixed-bottom">
    <div class="container">
      <p>© Home Service Booking. All rights reserved. | Serving India with trusted home services.</p>
    </div>
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
