<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Booking Confirmed - Home Service Booking</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card shadow border-success rounded-4">
          <div class="card-header bg-success text-white text-center py-4">
            <h2 class="mb-2">✅ Booking Confirmed Successfully!</h2>
            <p class="mb-0 lead">Your request has been scheduled. We'll contact you soon.</p>
          </div>
          <div class="card-body p-4">
            <!-- Booking Details Box -->
            <div class="row g-3 mb-4">
              <div class="col-md-6">
                <div class="border p-3 rounded bg-light">
                  <h5><strong>Service Details</strong></h5>
                  <p><strong>Service:</strong> <%= booking.service.name %></p>
                  <p><strong>Price:</strong> <span class="text-success fw-bold">₹<%= booking.service.price %></span></p>
                </div>
              </div>
              <div class="col-md-6">
                <div class="border p-3 rounded bg-light">
                  <h5><strong>Schedule</strong></h5>
                  <p><strong>Date:</strong> <%= new Date(booking.date).toLocaleDateString('en-IN') %></p>
                  <p><strong>Time Slot:</strong> 
                    <span class="badge bg-info fs-6 px-3 py-2">
                      <%= booking.timeSlot.charAt(0).toUpperCase() + booking.timeSlot.slice(1) %> (e.g., 9 AM - 12 PM)
                    </span>
                  </p>
                </div>
              </div>
              <div class="col-12">
                <div class="border p-3 rounded bg-light">
                  <h5><strong>Customer Information</strong></h5>
                  <p><strong>Name:</strong> <%= booking.customerName %></p>
                  <p><strong>Contact Method:</strong> 
                    <% if (booking.contactMethod === 'email') { %>
                      <span class="badge bg-primary fs-6">📧 Email ID</span> - <%= booking.email %>
                    <% } else { %>
                      <span class="badge bg-success fs-6">📞 Phone Number</span> - <%= booking.phone %>
                    <% } %>
                  </p>
                  <p class="text-muted small">We'll reach out via your preferred method to confirm or reschedule if needed.</p>
                </div>
              </div>
            </div>

            <!-- Action Buttons Box -->
            <div class="text-center border-top pt-3">
              <a href="/bookings" class="btn btn-primary btn-lg me-3">View All Bookings 📋</a>
              <a href="/services" class="btn btn-outline-primary btn-lg">Book Another Service 🔧</a>
              <a href="/" class="btn btn-outline-secondary btn-lg">Back to Home 🏠</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
