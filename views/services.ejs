<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Our Services - Home Service Booking</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-light">
  <!-- Navbar (same as index) -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <a class="navbar-brand" href="/">🏠 Home Services</a>
      <div class="navbar-nav ms-auto">
        <% if (user) { %>
          <span class="navbar-text me-3">Welcome, <%= user %>!</span>
          <a class="nav-link" href="/services">Services</a>
          <a class="nav-link" href="/bookings">My Bookings</a>
          <a class="nav-link" href="/logout">Logout</a>
        <% } else { %>
          <a class="nav-link" href="/services">Services</a>
          <a class="nav-link" href="/login">Login</a>
          <a class="nav-link" href="/signup">Sign Up</a>
        <% } %>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <% if (user) { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        Welcome back, <%= user %>! Ready to book a service?
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>

    <h2 class="text-center mb-4">Our Services 🔧</h2>
    <% if (services.length === 0) { %>
      <div class="alert alert-info text-center">
        No services available yet. Check back soon!
      </div>
    <% } else { %>
      <div class="row">
        <% services.forEach(service => { %>
          <div class="col-md-4 mb-4">
            <div class="card h-100 shadow">
              <div class="card-body">
                <h5 class="card-title"><%= service.name %></h5>
                <p class="card-text"><%= service.description %></p>
                <p class="text-primary fw-bold">₹<%= service.price %> / Service</p>
                <% if (user) { %>
                  <a href="/booking?serviceId=<%= service._id %>" class="btn btn-primary w-100">Book Now</a>
                <% } else { %>
                  <a href="/login" class="btn btn-outline-primary w-100">Login to Book</a>
                <% } %>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    <% } %>
  </div>

  <!-- Footer -->
  <footer class="bg-dark text-white text-center py-3 mt-5 fixed-bottom">
    <p>&copy; 2024 Home Service Booking. All rights reserved.</p>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
